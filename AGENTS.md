# Sandbox MOPD Lab

本目录是独立 Git 仓库，不是父项目子模块。先读 README.md、README链接的当前实施计划、docs/EXPERIMENTS.md 和 docs/BUDGET.md。当前状态以本仓库文档和真实证据为准，不继承父项目模型、数据、环境或运行授权。

- 所有 Lab 文件放本目录；模型、缓存、密钥、环境、运行产物保持忽略。不要提交秘密或大文件。
- Mac 日常办公至少保留 12 GiB，两个实验合计最多 12 GiB。本 Lab 不新增 Mac 常驻负载或本地模型/沙箱；优先 home-5090 模型与 Daytona 大规模 CPU 沙箱，Modal 做小规模对照和 GPU 候选。账户 credits 不代表配额或无限运行授权。
- 远端操作遵循 HOME-5090 手册；共享设施保持通用、最小化，不擅自修改部署或停止其他任务。
- home-5090 与其他同事共享。运行前只读检查资源，仅管理已核实属于本 Lab 的精确任务 ID/PID；不得为腾资源杀死、重启或暂停他人进程。资源不足或归属不明时停止申请新负载，向用户报告并由用户协调。
- 先注册资源、判据、超时和清理方案，再运行；区分配置存在、真实执行、限额验证和学习效果。失败不得改阈值自证。
- 用户已授权：每批工作完成并验证后，按具体路径暂存，提交并推送到本仓库 origin。禁止 force push，不包含他人未确认改动。不提交或推送父仓库。
- origin 指向 ChaoyuWang04/Sandbox-MOPD-lab；不要推到父仓库的 remote。远端失败如实报告，不能把本地提交写成推送成功。
