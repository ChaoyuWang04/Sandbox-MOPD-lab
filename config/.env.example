# 后续入口须从自身位置解析 LAB_ROOT 并显式设置所有目录。
# 这是配置模板，尚无程序消费；不要直接 source 后假定已生效。
LAB_ROOT=
LAB_MODEL_ID=Qwen/Qwen3-4B
LAB_MODEL_REVISION=
LAB_INFERENCE_BASE_URL=
WANDB_PROJECT=sandbox-rl-lab
WANDB_API_KEY=
# 以下变量必须由入口设置为 LAB_ROOT 下的绝对路径，不能依赖默认 HOME。
HF_HOME=
UV_CACHE_DIR=
PIP_CACHE_DIR=
XDG_CACHE_HOME=
TORCH_HOME=
TRITON_CACHE_DIR=
VLLM_CACHE_ROOT=
WANDB_DIR=
WANDB_CACHE_DIR=
WANDB_CONFIG_DIR=
WANDB_DATA_DIR=
TMPDIR=
