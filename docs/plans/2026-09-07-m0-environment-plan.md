# M0 环境实施计划

## 当前状态：M0 已完成

2026-09-08用户明确取消M0-G4“从零到全依赖就绪≤20分钟”。这是验收范围修订，不是把历史失败改成通过；当前不换机器，不为部署速度重复下载模型或重建环境。

| Gate | 生效判据 | 验收 |
|---|---|---|
| G1 | Qwen3-4B，16k上下文，实际4096输出<90秒；正确工具调用 | PASS：12288输入+4096输出，30.75秒，add_numbers(17,25)正确 |
| G2 | 用户授权16不满足后采用8并发，创建+回收<60秒 | PASS（8并发降级）：8/8，4.97秒，已回收；不声称16通过 |
| G3 | 官方Harbor示例完整生命周期与正负对照 | PASS：NOP=0、oracle=1，正常阶段隔离与回收通过；非强对抗隔离证明 |

精确commit、plan/run、状态SHA及原始产物位置只维护在[EXPERIMENTS](../EXPERIMENTS.md)。M0通过不代表M1任务池、训练、故障注入或大规模稳定性已经验收；当前未启动M1。

## 已交付与保留边界

- 独立Git仓库及Lab模型、环境、缓存、任务和证据；不导入父项目业务栈。
- Mac只做代码、控制和短时检查，不新增本地模型、沙箱或常驻服务；办公至少保留12GiB。
- home-5090普通prepare/probe两个固定入口，依赖锁和模型revision固定；Python头文件在Lab内私有解包并校验，真实Triton编译已通过，不改系统Python/驱动。
- 模型、依赖和大日志留在5090独立data根；Daytona优先CPU沙箱，Modal历史失败不混用为Daytona通过证据。
- 单卡probe预算与资源限制保持原配置；只清理自身精确进程，资源不足不影响其他同事。预算和限制以[BUDGET](../BUDGET.md)为准。

## 冷检查取消与收尾

取消第三个冷recipe接入请求；服务器负责人确认从未注册/部署。冷运行未提交，不做额外下载，不清空已有环境。删除未使用的冷入口及专用测试，Git可恢复；普通prepare输出中的历史冷耗时元数据仅为记录，不再用于M0晋级。自动跟进m0已暂停，提示已改为不得恢复旧冷计划。

本批提交推送只含独立Lab明确路径，不修改或推送父仓库。保留失败与成功原始证据；当前hlab无活动Lab任务。

## 后续阶段前需要澄清（未开工）

- M1的k/8是八次采样成功比例，不等于“至少一次成功”的pass@8。
- 正常agent阶段私有材料不可见，不证明恶意后台进程跨阶段不能偷读；强对抗隔离另行设计。
- M3需预先冻结6次运行完整性、故障注入单位与反事实成功的测量方法。
- M4热切adapter的梯度归属、KL方向和显存容量需要实际验证。
