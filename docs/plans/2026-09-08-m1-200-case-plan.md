# M1：200题扩池执行与跟踪

2026-09-08用户批准，并随后明确要求持续完成自建、下载与200题准备，中途不重复确认既有范围。设计唯一权威为[总计划M1/M4](../sandbox-rl-lab-plan.md)，本文件只维护执行顺序与产物。200题是试验规模，不保证学习有效。本轮执行P1～P3；模型基线和训练不由“题目准备好”自动授权。

## 当前基线与旧证据

- 现有自建32题由规则模板生成；本地实例在`data/m1/frozen-v1/tasks/`，清单`configs/m1-pool-manifest.json`，SHA256为`66a844924f96d3de135e8e3040792a4abf084b37e3c21234f7834132c7b830fc`。不改这些字节、ID或旧16题eval用途。
- 自建48已在`data/m1/v2/self48/tasks/`生成（约2.5MiB），对应manifest SHA256 `3f13ba82d7d5f657956c578402bc9ebc2aa87435b29ba11690c7af3fe640303a`。新增四族各4题：配置优先级、资源生命周期、异步依赖、原子替换；旧32保留。统一200题文件已装配，逐题云验收尚未完成；M0官方hello-world不是TB池。
- 旧32题pilot创建过1个沙箱，模型请求0，失败后独立查询确认已回收；修复离线测试通过但未真实复跑。精确run、哈希与消费记录只在[EXPERIMENTS](../EXPERIMENTS.md)维护。
- 旧campaign保持锁定，不重置尝试数、费用或unresolved_cleanup，不重复提交已消费plan。原[小池计划](2026-09-08-m1-small-pool-plan.md)仅解释旧运行，264创建/$10不是本次扩池预算。

## 顺序与交付

以下路径为计划产物，未打勾不表示实现。所有可复用逻辑在`lab_runtime/`，`scripts/`仅固定入口；先TDD，再规格与质量审阅。沿用当前独立M1 worktree，不改父仓库。

### P1：来源与候选清点（无模型/GPU）

- [x] 创建`configs/m1-sources-v2.json`固定来源清单：SWE-smith与Gym-Lite parquet官方SHA/size、TB2与Harbor上游commit、只下载不安装的PyArrow wheel；README留作许可证元数据。逐题底层仓库许可证仍需随候选清点，不能按代码工具许可证推断。SWE-smith patch是制造bug，Gym patch是修复，两者不混用。
- [x] 建立`lab_runtime/task_sources.py`与`tests/test_task_sources.py`，固定入口`scripts/m1_import.py`只接受已登记来源；10项离线测试、规格与质量复审通过，含来源身份复用、路径/链接拒绝、1MiB流式解包、校验与deadline。远端下载已完成，任务答案隔离由P3适配器验证。
- [x] 增加`lab_runtime/source_catalog.py`：先核对所有parquet的SHA/大小，再以16行批次输出原始JSONL与来源/patch/测试数索引，输出总量≤8GiB。SWE-smith没有base_commit，保留null及image_name，不伪造与Gym相同的身份；9项测试及两级审阅通过，真实PyArrow解码已完成。为严格combo补缺，固定完整SWE-Gym revision `bb94ed9e39bbeb96a7fcbfb533b80f25a7fd59cb`（2438题、parquet43644473字节），不改变Gym50配额；Lite不能仅凭Git展示/参数解析凑组合题。
- [x] `scripts/m1_catalog.py`与`lab_runtime/catalog_run.py`提供home5090固定CPU入口：1200秒，独立PyArrow wheel身份venv，仅离线安装已验wheel，不修改生产环境；receipt/路径/bytecode防护经11项测试及两级复审。Home recipe尚未部署，实际远端解码已由[BUDGET](../BUDGET.md)登记的Modal短CPU备用完成，不替换默认Daytona题目沙箱。
- [x] 候选分类/重复组/资源画像在`data/m1/v2/research/candidate200-annotations.json`，200个唯一ID及80/20/100配额已核对；SWE资源是计划请求值，不是实测峰值。Smith使用公开实例，Gym使用真实issue，没有新开模型造bug。逐题文件哈希和最终可运行状态仍由后续装配/对照填入。
- [x] 已核对HOME-5090固定入口、磁盘与网络；recipe未登记时未绕过控制器，转用预登记Modal CPU备用完成19资产校验、选定归档解包及61804行catalog。精确App、计数、SHA见[EXPERIMENTS](../EXPERIMENTS.md)；完整数据留远端，Mac只存代码、清单和小型审阅材料。后续题目镜像/依赖及逐题对照仍未完成，不将云预算或GPU权限从旧pilot继承。

### P2：自建48与统一元数据

- [x] 独立`lab_runtime/task_pool_v2.py`新增16题；v1源码不变，440原文件逐字节相等。规格和质量审阅通过；提前exit0、None默认值、复制资源对象反例先RED后GREEN。仅离线结果，不算真实沙箱验收。
- [x] `configs/m1-pool-v2.json`登记总计划要求的主次能力、机制、来源生成方式、资源、teacher_route、split、leakage_group与分类理由；产出来源×能力×split交叉表。
  - 元数据校验器`lab_runtime/pool_manifest_v2.py`已完成，7项离线测试及规格/质量审阅通过：来源身份不可重复、旧24A/8B及新增16B映射固定、精确配额与切分、旧哈希和组隔离检查。统一组装已将该检查用于实际200题；结果仅为`metadata_validated`，不表示运行通过。
- [x] 内容标注与直接repo/function/PR/patch组切分已进入统一清单，未做模型筛选。保留旧eval16的同模板诊断标签；Gym历史改名/移动语义去重未穷尽，限制随清单保留，不宣称完全语义无重叠。资源画像仍为请求值，真实不兼容将在P3明确报告。
- [x] `scripts/m1_build_pool.py`按`configs/m1-build-inputs-v2.json`固定25项输入身份，实际装配`data/m1/v2/tasks/`200目录、2311文件、47256519字节，全部经Harbor0.22加载。旧440文件不变，TB50只在task.toml追加镜像摘要，原始时限与其余字节不变；SWE完整许可副本在ignored provenance。8项unittest及两级审阅通过，重复构建相同。统一manifest SHA256 `d58c12973a9ff127b76fa73461bd9f9d8b9649bc7a428ba4128156fc40dda129`，状态仅`files_prepared_runtime_unvalidated`。
  - 首个SWE云对照暴露XPASS兼容问题后，新版本在`data/m1/v2/tasks-r2/`；当前入口默认`configs/m1-build-inputs-v2-r2.json`，当前清单`configs/m1-pool-v2-r2.json` SHA256 `c98a74d88d3641a02c8e9ce40505f9e4f1b26f17042aae43cab801367c600f5b`。10项版本检查与两级审阅通过、200题再次Harbor加载；48自建与50TB文件完全相同，102SWE每题只更新两份判分/pytest插件文件。原v2清单和任务文件完整保留，不覆盖旧云证据；当前仍不是逐题运行验收通过。
- [x] 对新16题完成离线oracle/NOP/错误解与字节确定性测试；当前独立self48产物在`data/m1/v2/self48/`，统一200题装配进入`data/m1/v2/tasks/`，不得覆盖frozen-v1。

### P3：真实Harbor适配与200题验收

- [x] `configs/m1-swe-profiles-v2.json`冻结Smith52/Gym50官方执行配置、63个不同镜像的公开摘要、精确repo/version及mypy特殊测试选择；元数据两级审阅通过，6项离线检查通过。新checkout独立fixture测试可运行，缺研究资产时5项源集成检查明确skip；不能算源复核通过。尚未拉取或运行题目镜像。
- [ ] 在`lab_runtime/`实现来源适配，测试真实Harbor任务加载及源测试契约；测试数据不得含密钥，修复patch/参考解不进入agent观测。
  - 实现顺序：纯判分函数 → Harbor题目构造/私有测试恢复 → 云端装配 → 小批及全池对照。固定Gym harness来源是`SWE-Gym/SWE-Bench-Fork@242429c188fcfd06aad13fce9a54d450470bf0ac`，并非同名数据仓库；两种来源的官方`test_passed`都允许PASSED/XFAIL。
  - 测试身份按固定原生parser的首个空白token投影，不用前缀模糊匹配。记录完整collection和终态；多个完整node映射同一原生键时必须全部通过，缺失、收集失败或协议未完成单独记invalid，不当模型零分。该碰撞保护比上游字典最后写入覆盖严格，报告必须保留差异。
  - XPASS是已知非通过终态，不是未知协议；PASSED/XFAIL通过集合不变。必需测试XPASS得0，非计分测试XPASS只记录，不使完整运行无效。37项判分/真实pytest回归与两级审阅通过，旧云464节点只读重放的10个失败计分键未变；必须用新版本任务真实复跑，不能追改旧reward=null。
  - `lab_runtime/swe_grading.py`纯判分部分已通过规格/质量复审，15项判分加6项配置测试通过；全部收集节点必须有终态（包括非计分节点），无inventory不能出有效分数。真实pytest收集、隔离与判分调用仍待接线，不能将函数测试当逐题通过。
  - `swe_pytest_plugin.py`与`swe_test_runner.py`已在微型真实pytest子进程接通上述协议，18项运行器检查通过；覆盖正常失败/修复、teardown错误、缺测试、提前退出、nonce、多session、超时和专属进程组清理。2026-09-08主任务全套185项测试通过（25.061秒），规格和代码质量复审通过。源镜像内接线/逐题运行仍待完成。测试专用pytest8.4.1位于Lab根`.venv-test-swe/`，用`SWE_TEST_PYTHON`指定；生产venv未加依赖，缺该测试依赖的干净checkout会明确skip这组真实子进程测试。
  - Smith先固定实例分支，私有保存恢复被移除测试的差异，再移除可泄漏答案的原Git历史；oracle逆向应用bug patch。Gym使用逐题镜像、正向gold patch，test_patch仅验证阶段上传。任何安装/恢复操作不得覆盖已完成的实现修复；需要重装时先保存修复并验证重放身份。
  - `swe_tasks.py`/`swe_verify.py`构造与私有验证准备通过两级复审。102条冻结SWE记录均实际生成并经Harbor0.22加载，题面原始字节相等（Harbor读取时正常统一CRLF）；当前全套200项代码测试通过（28.417秒），不是200题云验收。Smith52分支已由GitHub精确ref解析为52个不同commit并写入profiles，不再靠可移动分支选题；所有102个profile带显式instance_id防止同仓库错配。下一步接入私有receipt、原Git历史移除和真实云生命周期。
  - `swe_workspace.py`/`swe_hooks.py`私有receipt与历史隔离已通过两级复审、10项本地Git/真实Harbor钩子注册检查。仅在验证过原commit的disposable testbed中移除旧.git，工作文件不变，重新创建单根历史；Smith恢复测试差异只返回控制器、到验证阶段才上传。正常phase隔离已在夹具验证，云端行为仍待首批。
  - TB50原始任务已在`data/m1/v2/tb50-source/`从固定归档流式解包，422文件（含README）、41330822字节逐一对照远端导入receipt哈希，50题全部Harbor加载且均声明预构建镜像。Mac新增约40MB原始归档与41MB选定源码，仅用于阅读/装配；没有本地执行任务或启动镜像。
- [ ] 将资源画像写入[BUDGET](../BUDGET.md)，登记每批创建数、并发、时限、费用上限/预留、持久存储和清理方案；按配额和任务需求确定，不能机械沿用1GiB/5分钟。GPU任务不进入默认CPU批次。
- [ ] 新建v2身份隔离的有界campaign，关联旧失败证据且保留历史预算消费；测试未知创建/清理失败会阻止后续创建。更换固定入口需核对hlab recipe并生成新的精确plan。
- [ ] 按下述分层方案完成代表性接入验证；取消全池200题逐题双对照门槛。文件清单SHA已冻结，运行覆盖单独记录；失败分为环境/验证器/任务问题，不以模型得分筛final-test。

### P3分层验证（用户批准替代全池双对照）

**目标：** 200题资产齐全、全量轻量检查、自建批量逻辑检查、共享适配器回归及代表性Daytona接入验证通过后，可进入预注册小规模模型pilot。未抽查题保持“目标平台未逐题复验”，不宣称200题全部云端通过。M1完整收尾仍需要P4，M2/RL不在本次范围。

**证据分层：**
- 全部200题：固定来源/任务哈希、Harbor加载、配额/切分/隔离组及SWE执行配置检查；继承上游任务与镜像身份，不能把公开资产存在性当成本机运行证明。
- 自建48题：现有规则生成器测试批量覆盖每个实例的oracle、NOP/未修复和错误解，保留独立边界/变异反例；不重复为每个参数变体创建两个云容器。
- 共享适配：现有pytest微夹具覆盖collection失败、缺失测试、XPASS、提前退出、超时及清理；原始云日志重放检查评分一致性。原生键碰撞采用更严格规则的差异继续明确，不宣称完全等同上游。
- 云接入：按source、镜像/仓库、测试命令、准备/重装流程和私有恢复路径报告分组；区分“共享代码路径已抽查”和“具体镜像未运行”。复用self/Smith/Gym六次有效对照；补缺优先覆盖新执行分支，而不是为每个镜像自动设双对照门槛。TB尚缺本池代表性对照，M0 hello-world不替代TB任务。
- 异常只扩大受影响分组；已确认回收的失败不阻挡无关题。未知创建或清理仍暂停新建。新增云批次先登记精确样例、资源、时限、费用和回收路径；没有自动全量复跑。

**执行步骤和文件职责：**
1. 就地更新本计划、总计划M1-G2、BUDGET、EXPERIMENTS、README；撤销原394次新增/$180预留与固定2并发路线，不重写旧失败/消费账本。未上线的全池控制计划保留为不可执行历史草稿，不继续实现control_job/control_pool。
2. 用现有入口复核200任务文件/Harbor解析，重跑task_pool/task_pool_v2、pool_manifest、SWE配置/评分/运行器/hooks及组装测试。将结果、版本和缺口写入ignored artifacts；完整测试不是全部云任务通过。
3. 只读盘点已安装Harbor0.22的Job/并发接口，生成按执行路径的覆盖表，显式列出未覆盖分支。优先原生Job和薄层资源/回收保护，不新增专用调度器；新增功能须TDD后审阅。先制定小型代表批次，不借此恢复全池400次。
4. 完成代表批次后汇总“资产/逻辑/适配回归/目标平台样例”四种状态。正式发布adapter或严格全量平台认证属于另行任务。

**后续模型/RL诊断契约：** reward只在测试确实完成时解释。每次记录execution_status、verifier_status、reward、failure_category和有效测试数；环境/收集/超时/缺奖励属于invalid或基础设施失败，不映射成模型0分。报告有效reward分布及无效率，按来源/执行分组聚合；全零/全一、突变只触发诊断，不直接证明模型能力或判分器正确。final不用于训练或依据reward改题/选题。

**方法依据（2026-09-08）：** [SWE-smith验证](https://swesmith.com/guides/harnesses/)明确生成阶段测试验证和gold sanity；[SWE-Gym](https://github.com/SWE-Gym/SWE-Gym)提供预构建环境；[Harbor adapter认证](https://www.harborframework.com/docs/datasets/adapters)建议新adapter全量oracle，但本次仅内部pilot准备，不宣称达到发布认证；[Harbor Job](https://www.harborframework.com/docs/core-concepts)已有并发调度，在线文档接口须与固定安装版本核对。

### P4：模型基线与M1收尾

- [ ] 冻结真实推理的逐题上下文/时间/解码预算与采样次数，先登记预算，再按精确plan门控运行；home5090资源不足交用户协调，不动其他进程。
- [ ] 先少量train/dev多轮pilot，确认成功轨迹、可靠判分与清理后再扩量；保存所有失败及未知usage，不静默补样。
- [ ] 仅train/dev做学习信号与难度调查；选A/B各8组成overfit_16；最终测试只跑预注册base基线并封存逐题结果。
- [ ] 将真实资产/对照覆盖/模型轨迹/基线分别计数写回EXPERIMENTS与预算。完成总计划M1-G1～G5且审阅后提交、推送并打阶段tag；M2真实训练另行登记与批准。

## 验证方法与停止边界

本阶段离线检查使用Lab现有venv；新模块分别执行`python -m unittest tests.test_task_sources`等实际落地后的测试入口，最后全套发现测试与`git diff --check`。拟定命令不是当前已有功能证明。配额表总和应为200、训练80/dev20/final100；每个ID仅一个split，旧16 eval仍在final，TB无train/dev，组合/OOD无train，原32内容哈希不变。

新来源需预先核对许可证和原资产再分发权限；不能只按工具代码许可证推断数据/仓库许可证。候选存在歧义先标注/核验，不用语言模型臆测成已验证分类。下载或测试资源不能有界估计、共享资源不足、清理不确定或需要新系统权限时停在对应步骤，保留已完成成果。数量不足如实报告；不得放宽来源、隔离或可运行标准凑齐200。

此计划只承诺M1扩池与基线；M4 joint对照、嵌套规模曲线和云训练不是本次运行授权。200之后根据train/dev证据决定扩量，不自动改为2k题。
