[project]
name = "sandbox-mopd-m0-serving"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = ["vllm==0.28.0", "harbor[daytona]==0.22.0"]

[tool.uv]
package = false
environments = ["sys_platform == 'linux' and platform_machine == 'x86_64'"]
